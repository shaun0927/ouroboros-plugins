# Schema migration guides

Migration guides live here when a MAJOR schema version bumps. See
[`docs/contract.md` Versioning](../contract.md#versioning) for the
policy.

| From → To | Guide |
|---|---|
| (none yet — v0.x is the initial schema, no migrations to write) | — |

When `1.0` ships, this directory will gain `0-to-1.md` describing the
breaking changes and how to update existing plugin manifests.
